# User Service

Manages staff and driver profiles, assignments, and user-related operations.

## Configuration
- Port: 8002
- Database: PostgreSQL
- Key Endpoints:
  - GET /users/{user_id}
  - POST /users
  - PUT /users/{user_id}
  - POST /drivers
  - GET /drivers
